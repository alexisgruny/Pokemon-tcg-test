<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/styles/styles.css">
    <link rel="stylesheet" href="/styles/navbar.css">
    <link rel="stylesheet" href="/styles/footer.css">
</head>

<body>
    <%- include('partials/navbar') %>
    <div class="card-detail<%= type || 'default-type' %>">
        <h1>
            <%= name || 'Nom non disponible' %>
        </h1>
        <% if (image) { %><img
            src="<%= image %>/high.webp"
            alt="<%= name || 'Image non disponible' %>"
            width="300">
        <% } else { %>
        <p>Image non disponible</p>

        
        <% } %>
        <p><strong>#</strong>
            <%= localId || 'ID non disponible' %>
        </p>
        <p>
            <%= description || 'Description non disponible' %>
        </p>
        <% if (setLogo) { %><img
            src="<%= setLogo %>.webp" alt="Logo du set"
            width="100">
        <% } else { %>
        <p>Logo du set non disponible</p>

        
        <% } %>
        <p>
            <%= illustrator || 'Illustrateur inconnu' %>
        </p>
        <% if (rarity) { %>
        <p class="<%= rarity %>">Rareté :
            <%= rarity %>
        </p>
        <% } else { %>
        <p>Rareté non disponible</p>

        
        <% } %>
        <% if (isAuthenticated) { %>
        <div class="card-counter">
            <button class="decrement">-</button>
            <span class="quantity">0</span>
            <button class="increment">+</button>
        </div>

        
        <% } %>
    </div>
    <a href="/cards/set/<%= setId || '#' %>">Retour à la liste
        des cartes</a>
    <%- include('partials/footer') %>
</body>

</html>